# Copy to .env and fill in. Used by config.settings.local
SECRET_KEY=your-secret-key

# PostgreSQL (optional; if unset, local uses SQLite)
DATABASE_URL=postgres://moznods:moznods@127.0.0.1:5432/moznods

# Invite code required for registration (single shared code for MVP)
REGISTRATION_INVITE_CODE=moznods

# CELERY_BROKER_URL=redis://127.0.0.1:6379/1
# CELERY_RESULT_BACKEND=redis://127.0.0.1:6379/2
